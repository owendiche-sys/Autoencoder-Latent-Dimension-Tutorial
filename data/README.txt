This project uses the Fashion-MNIST dataset.

The dataset is downloaded automatically in the notebook using `torchvision.datasets.FashionMNIST` when the notebook is run in Google Colab or another Python environment.

No manual dataset download is required.

If the dataset files are not already present, they will be fetched automatically during execution.